# Input saved in ~/.config/chezmoi/chezmoi.toml

[data]
  machine_profile = {{ promptString "Machine profile (workstation|server)" | quote }}
  name = {{ promptString "Full name" | quote }}
  email = {{ promptString "Email" | quote }}
  githubUser = {{ promptString "GitHub username" | quote }}
