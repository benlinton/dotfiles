#!/usr/bin/env bash

# Output filesize of everything in pwd
alias du-pwd='du -hd 1'

# Colorized, special characters (after dirs, executables, symlinks, etc.)
alias ls='ls -GF'

# List functions
alias functions-list='typeset -F'
