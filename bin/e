#!/bin/sh
#
# Shortcut to my favorite editor.
#
# USAGE:
#   $ e                         # => opens the current directory in your editor
#   $ e .                       # => opens the specified directory in your editor
#   $ e path/to/README.txt      # => opens file
#
# TODO: handle multiple args (and maybe gracefully handle paths with spaces?)

if test "$1" == ""
then
  exec $EDITOR .
else
  exec $EDITOR "$1"
fi
